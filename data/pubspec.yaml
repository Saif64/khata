name: data
description: The data layer for the expense tracker app.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  
  domain:
    path: ../domain

  
  hive: ^2.2.3

  # supabase: ^2.7.0 # Replaced by supabase_flutter for consistency with AuthRepositoryImpl
  supabase_flutter: ^2.9.0 # Matching presentation layer, or use compatible version.
                           # Ensure this provides the SupabaseClient AuthRepositoryImpl expects.
  
  equatable: ^2.0.7
  fpdart: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  flutter_test: # Added for completeness, though 'test' might be sufficient for non-widget tests
    sdk: flutter
  mockito: ^5.4.4
  build_runner: ^2.4.15
  hive_generator: ^2.0.1
  test: ^1.21.0 # Already present
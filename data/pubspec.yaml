name: data
description: The data layer for the expense tracker app.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  
  domain:
    path: ../domain

  
  hive: ^2.2.3

  supabase: ^2.7.0
  
  equatable: ^2.0.7
  fpdart: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  build_runner: ^2.4.15
  hive_generator: ^2.0.1
  test: ^1.21.0